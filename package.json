{
  "scripts": {
    "start": "cd my-blog-backend && npm start",
    "build": "cd my-blog && npm install && npm run build && cd ../my-blog-backend && npm install && npm run build && cd ..",
    "preinstall": "cd my-blog-backend && sudo npm install -g nodemon && cd ..",
    "dev": "cd my-blog-backend && nodemon",
    "postinstall": "cd my-blog-backend && npm install && npm start"
  }
}
